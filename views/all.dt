extends ./layout

block content
  p#desc Alle #{llen} bekannten Zitate.
  div#citebody
    - long id = start;
    - foreach(string cite; cites)
      div.citecontainer
        div.citeid #{id}
        - id -= 1;
        div.cite #{cite}
        div.clear
